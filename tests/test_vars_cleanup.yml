---

container_default_basedir: "."

containers:
  mycontainer:
    hosts: ["all"]
    name: "mytestcontainer"
    image: "drpsychick/dnsmasq"
    directory: "dnsmasq"
    remove: true
  mybuild:
    hosts: ["all"]
    name: "mybuildtest"
    image: "mybuildtest"
    directory: "mybuildtest"
    cleanup: true
    purge: true
