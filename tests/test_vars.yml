---

container_default_basedir: "/notexists"

containers:
  mycontainer:
    hosts: ["all"]
    name: "mytestcontainer"
    image: "drpsychick/docker-dnsmasq"
    directory: "dnsmasq"
    pull: yes
    create: yes

